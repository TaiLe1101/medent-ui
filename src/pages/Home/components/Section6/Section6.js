import classNames from 'classnames/bind';
import styles from './Section6.module.scss';

const cx = classNames.bind(styles);

function Section6() {
  return <section className={cx('wrapper')}>section6</section>;
}

export default Section6;
