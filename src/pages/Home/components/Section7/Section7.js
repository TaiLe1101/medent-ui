import classNames from 'classnames/bind';
import styles from './Section7.module.scss';

const cx = classNames.bind(styles);

function Section7() {
  return <section className={cx('wrapper')}>section7</section>;
}

export default Section7;
